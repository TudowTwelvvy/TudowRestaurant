<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="TudowRestaurant.Pages.OrdersPage"
             xmlns:vm="clr-namespace:TudowRestaurant.ViewModels"
             xmlns:data="clr-namespace:TudowRestaurant.Data"
             xmlns:models="clr-namespace:TudowRestaurant.Models"
             xmlns:controls="clr-namespace:TudowRestaurant.Controls"
             Shell.NavBarIsVisible="True"
             Background="White">

    <Grid
            BackgroundColor="White"
            RowDefinitions="Auto, *">
        <Grid Grid.Row="0" ColumnDefinitions="*, Auto">
            <Label
                    Grid.Row="0"
                    FontAttributes="Bold"
                    FontSize="25"
                    HorizontalOptions="Center"
                    HorizontalTextAlignment="Center"
                    Text="Selected Order Items"
                    TextColor="Black" />
            <ImageButton
                    Grid.Column="1"
                    Margin="0,0,5,0"
                    BackgroundColor="{StaticResource SecRed}"
                    
                    CornerRadius="5"
                    HorizontalOptions="End"
                    MinimumHeightRequest="20"
                    MinimumWidthRequest="20"
                    Source="x.png"
                    ToolTipProperties.Text="Clear Selection"
                    VerticalOptions="Center" />
        </Grid>

    </Grid>
</ContentPage>